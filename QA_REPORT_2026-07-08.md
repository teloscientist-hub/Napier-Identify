# Phone App Mock Prototype QA Report - 2026-07-08

Source checklist: `QA_CHECKLIST.md`

## Runtime

- `npm install` had already completed.
- `npm run typecheck` passes.
- Installed Watchman with Homebrew.
- `npm start -- --localhost` now starts Metro at `exp://127.0.0.1:8081`.
- Added Expo-matched web QA packages because this machine did not report an available iOS simulator:
  - `react-native-web`
  - `react-dom`
  - `@expo/metro-runtime`
- `npm run web -- --localhost` builds successfully at `http://localhost:8081`.

## QA Result

Status: pass with wording/checklist mismatches.

The mock prototype works end to end on the web target:

- Identify tab opens without login.
- Take Photo routes to capture review.
- Capture review shows Use Photo, Crop / Isolate Object, and Retake.
- Crop screen routes to optional hints.
- Skip Hints runs mock search.
- Results show ranked candidates with strong, variant, and similar confidence treatments.
- Candidate row opens detail.
- Detail screen shows Napier reference facts, source citation, variants, related pieces, and separate private notes.
- Save To Collection works.
- Collection shows the saved item.
- History shows the searched/saved item.
- Settings shows privacy controls and prototype API mode.
- Failure mode shows a non-dead-end search error state.
- No-match state shows retry guidance and can return to mock matches.
- Language guardrails passed: the tested result screens did not claim verified status, authenticity, value, price, condition, completeness, or similar authority.

## QA Notes For Mark

These are not blocking prototype failures, but Mark should decide whether the checklist or UI language should be aligned before the next polish pass.

- Checklist says `Crop`; UI says `Crop / Isolate Object`.
- Checklist says `Possible Matches`; UI says `Ranked candidates`.
- Checklist expects an `Open Detail` button; UI opens detail by tapping the candidate row.
- Checklist says `User Notes`; UI says `My Notes`.
- Checklist says `Related Pieces`; UI says `Related pieces`.
- Checklist says `Search History` and `Mock query`; UI says `History` and shows the result directly.
- Checklist says `Prototype API Mode`, `Mock success`, and `Mock failure`; UI says `Prototype mode`, `mock`, and `failure`.
- Checklist says `Retain uploaded images`; UI says `Retain uploaded search images`.
- Checklist says `Model improvement`; UI says `Allow model improvement`.
- Checklist says `Strip EXIF location data`; UI says `Strip EXIF metadata`.
- Checklist says `Search Error`, `Try Again`, and `Return To Identify`; UI says `Search error`, `Try Another Photo`, and `Refine Search`.
- Checklist says `No confident match yet` and `Try a tighter crop`; UI says `No confident match` and `Crop closer to the jewelry`.

## Follow-Up

Recommended next step: refactor `App.tsx` into `screens/`, `components/`, `services/`, and `styles/` while preserving current behavior exactly.
